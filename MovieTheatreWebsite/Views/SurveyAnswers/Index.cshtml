@model IEnumerable<MovieTheatreDatabase.SurveyAnswers>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurveyQuestionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionOptionEnums)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SurveyQuestionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionOptionEnums)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SurveyAnswersId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SurveyAnswersId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SurveyAnswersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
