@model IEnumerable<MovieTheatreDatabase.SurveyQuestion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionType)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.SurveyQuestionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SurveyQuestionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SurveyQuestionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
